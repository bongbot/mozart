- path = new_admin_tag_path

= form_for([:admin, @tag], :html => one_submit_only, :remote => true) do |f|
  = f.error_messages
  = render :partial => "top_section", :locals => { :f => f }
  .buttonbar
    = f.submit_success t(:create_tag), :id => :tag_submit
    = link_to_cancel path
