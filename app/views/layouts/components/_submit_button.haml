- label ||= ''
- edit ||= false

- hidden = edit ? "" : "hidden"
.buttonbar{:class => hidden}
  - if Setting.compound_address
    = f.submit t(label), :onclick => "crm.clear_all_hints()"
  - else
    = f.submit t(label)
