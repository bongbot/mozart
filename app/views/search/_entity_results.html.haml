- model_name = model_name || ""
- model_names = model_name.pluralize
- models = entities

- if models.any?
  = render :partial => "#{model_names}/#{model_name}", :collection => models
- else
  %div{:style => "text-align: center"}
    = "Không có kết quả nào"
